contexts/daily_records/
# extracted session content from cross-workspace daily job — keep tool, drop output
periodic_jobs/cross_workspace_daily/extracted/
# agent_control_plane lives in its own repo
adhoc_jobs/agent_control_plane/
# audio artifacts from anki TTS pipeline
.anki_audio/
# agent-ops audit logs (may contain sensitive cluster info — commit selectively)
tools/agent_ops/logs/*.jsonl
.env
# agents/ — ignore everything except the SDK project (public portfolio)
agents/sre_oncall_triage_agent/
agents/CLAUDE.md
agents/excalidraw.log
# top-level agents symlink → work-harness, never track
/agents
archives/
*.log
tmp/
__pycache__

# secrets / tokens
github_token*
*token*.txt
*.pem
*.key

# scratch / transient
TODO/
.playwright-mcp/
input.txt
tmp.sh
tmp_resume.sh
tmp_*.sh

# work / internal materials — do not publish
work-contexts/
fix-ingress-webhook-ca.sh
ingress-nginx-admission.yaml
ingress-nginx-admission.yaml.bk
万字长文教你如何打造自己的 Harness Engineering*.pdf

# personal screenshots / images at repo root
/*.png

# 下线站点的线上产物快照（体积大、纯产物，不入库；说明见该目录 README）
work-contexts/toy-proj/_published_backup/*-gh-pages/

# Unsanitised oncall knowledge staged for the resume site: real customer names,
# cluster aliases and internal URLs. The project's own notes say it must never
# ship publicly — keep it out of this public repo entirely.
adhoc_jobs/dynamic_resume_site/knowledge_raw/
