export const background = 'generated_slides/slide_07_0.png';
export const backgroundSize = '100% 100%';

export const html = `
  <aside class="notes">
    [Start Live Demo: Cursor]
    Now, let's use Cursor with the actual CSV files.
    Watch the Feedback Loop.
    It writes code -> Error -> Reads Error -> Fixes Code.
    Result: It gives us a number: "33.1% non-export. 72% are Homeowners."
    The Trap: It works, but... do you trust it?
  </aside>
`;

export function initialize() {}
export function cleanup() {}
