export const background = 'generated_slides/slide_08_0.png';
export const backgroundSize = '100% 100%';

export const html = `
  <aside class="notes">
    The code ran. We got a number: 33.1%.
    But as a manager, do you trust it?
    The Trap: To verify this, you have to read the AI's code line-by-line.
    That is Micro-management. If I have to read every line of code, I might as well write it myself.
    This is where the "Tinkerer" hits a ceiling.
  </aside>
`;

export function initialize() {}
export function cleanup() {}
