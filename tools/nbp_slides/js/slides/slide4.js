export const background = 'generated_slides/slide_04_0.png';
export const backgroundSize = '100% 100%';

export const html = `
  <aside class="notes">
    Most people are stuck at L3 or L4.
    They use AI, but they don't trust AI.
    Today, we are going to climb to L6.
    We will solve the same "Hover Mystery" at each level to see the difference.
  </aside>
`;

export function initialize() {}
export function cleanup() {}
