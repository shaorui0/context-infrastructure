export const background = 'generated_slides/slide_06_0.png';
export const backgroundSize = '100% 100%';

export const html = `
  <aside class="notes">
    The AI isn't stupid. It's just an intern on Day 1.
    You didn't give it the database schema (Context), so it guessed to be "helpful".
    This is where most people quit AI analytics.
  </aside>
`;

export function initialize() {}
export function cleanup() {}
