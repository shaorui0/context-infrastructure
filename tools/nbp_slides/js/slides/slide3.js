export const background = 'generated_slides/slide_03_0.png';
export const backgroundSize = '100% 100%';

export const html = `
  <aside class="notes">
    Let's ground this in reality.
    Scenario: I'm a PM at Hover. VP asks: "Why do 33% of users build a model but never pay (export)?"
    Is our UI broken? Are they confused?
    We need to analyze this. I've generated a synthetic dataset of 5,048 users to solve this live.
  </aside>
`;

export function initialize() {}
export function cleanup() {}
